# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment files — never commit secrets
.env
.env.*
netlify.env

# TypeScript build cache
node_modules/.tmp/

# Editor & OS
.DS_Store
Thumbs.db
.vscode/settings.json

# Logs
*.log
npm-debug.log*

# Claude Code local config — not part of the project
.claude/

# Scratch files
*.docx
Images/

# Demo source drop-folder — raw inputs only; the processed assets in
# public/demo-media/ (committed) are what the app actually serves.
DEMO PHOTOS/
